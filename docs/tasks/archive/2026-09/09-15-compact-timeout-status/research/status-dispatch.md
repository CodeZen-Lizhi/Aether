Active task: docs/tasks/09-15-compact-timeout-status
You are already the trellis-implement sub-agent. Implement directly; do not spawn implement/check agents. User approved final plan with 实施 and task now in_progress. Load implement.jsonl/PRD/design via native injection or fallback. You own R1 only, independently alongside config/runtime agents.

Ownership: apps/aether-gateway/src/handlers/admin/observability/usage/{summary_routes,detail_routes}.rs and directly necessary observability helpers/tests (tests/control/admin/usage.rs); frontend/src/features/usage state helpers/composable/components/tests. No edits runtime/candidate loops, provider form/config, contracts, task artifacts/specs except short implementation report. No commits or production changes.

Bug evidence already located: admin_usage_terminal_candidate_state_override summary_routes.rs312 projects candidate failed/latency as request failure/latency for active usage; retry gap after watchdog can lack retryable marker at357. Frontend recordSync.ts131 terminal rank pins this erroneous failed state; status.ts242 pending+504 => failed while resolveTimelineFinalStatus288 => pending. Screenshots original first180s failure followed by retry, whole request300s failed. Candidate actualfailure must remain visible but logicalrequest active throughout retry. Existing lifecycle authoritative; don't infer requestfailed from singlecandidate. Preserve real terminal anti-stale protection, legacy no lifecycle fallback and image explicit failure. Need publicAPI lifecycle proof then frontend merge/polling tests; no simplistic let newer pending always overwrite failed.

Deliver R1 implementation and focused frontend tests, plus meaningful Router/SQLite test of failed candidate while usage active, subsequent retries and whole request terminal elapsed/error consistent in list/detail/trace. Add cases actual retry gap, not only concurrent pending candidate. Prefer existing fixtures. No cargo build until parent coordinates shared target lock; request test slot. Node tests may run independently. Browser fixture and final integration root coordinates. Read relevant frontend/backend error and quality specs; changes bound stated in approved docs. Report files/tests/limits concisely.
