# 实施顺序

用户已于 2026-09-12 回复「开始修改」。按本任务上下文实施，不使用 responsive-dialog-sizing 上下文。

## 准备

- [x] 核对当前代码、实际界面、顶部快捷切换和用户要求。
- [x] 写入 PRD、设计、研究证据与上下文清单。
- [x] 已交付最终方案，用户批准开始。
- [x] 开始时重查 git status；已有改动为桌面版本/锁文件及 output，实施期间保留。

## 实现

1. 删除偏好表单及专有加载/保存；保留顶部切换、Web 账号功能、旧重定向及时间逻辑。
2. 建立四类导航、URL 与旧 hash 兼容、窄窗口布局；保持草稿和弹窗状态。
3. 整理连接与启动，拆出诊断展示，保持恢复流程。
4. 合并记录与保留策略入口，区分删除期限与压缩时点；去掉批次编辑和保存提交。
5. 重排备份及配置迁移，维护操作和历史归入高级。
6. 整理兼容选项、Key 规则、版本及诊断，落实分组保存/取消和失败态。
7. 补齐英文映射，删除确认无引用的旧组件和说明。

不更改后端契约，不重构通用 CardSection 或其他页面。

## 验证

- 类型：`npm --prefix frontend run type-check`。
- lint：在 frontend 中运行 `npx eslint <本次修改文件>`，不跑全仓自动修复。
- 定向测试：`npm --prefix frontend run test:run -- src/desktop/__tests__/DesktopPreferences.spec.ts src/desktop/__tests__/DesktopDashboard.spec.ts src/desktop/__tests__/DesktopSettings.spec.ts src/views/admin/system-settings/__tests__/useSystemConfig.spec.ts`；按实际变更选择现有代理和导入测试。
- 最小新增行为测试：分类与旧 hash、切换保留草稿、保存失败、隐藏批次键不被提交、偏好表单删除后顶部切换仍可用。不断言任意 CSS 字符串。
- 浏览器隔离会话：切换并保留输入、保存刷新、代理测试、导入预览取消、维护确认取消，Web 无桌面操作。
- 布局：840x620、1024x768、1440x900、390x844，覆盖明暗和中英文；测量溢出并目视检查长名称、目录、展开表单及操作可达。
- 如桌面拆分影响调用链，隔离目录验证端口、自启动和恢复；不默认扩张到 Rust 全仓测试或安装包构建。
- 涉及副作用的真实清理/恢复只在隔离数据中验证受影响行为。

## 交付

- [x] trellis-check 子代理核对 PRD、保存目标、异步结果、路由草稿和隐藏值保持，无未修复发现。
- [x] 实施后更新前端设置布局与保存规范，记录四类导航、分组快照保存和隐藏批次键契约。
- [x] 汇报完成/未验证范围；提交、push、打包依会话授权处理。
