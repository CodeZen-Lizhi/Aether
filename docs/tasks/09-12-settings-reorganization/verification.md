# 验证记录

## 环境

- 源码：当前 frontend Vite 开发服务，127.0.0.1:5173。
- 网关：现有 target/release/aether-gateway，独立监听 127.0.0.1:18087，单独创建 SQLite 数据库。
- 运行目录：output/playwright/settings-runtime-n2jWFk，未使用用户的 8084 网关及数据库。
- 浏览器：独立 Playwright settings-qa 会话，使用本次创建的测试账号。
- 桌面展示检查使用显式模拟 IPC，HTTP 配置接口仍连接上述真实测试网关；不能等同于实际 macOS 原生命令验收。

## 已完成

- 偏好清理子任务：DesktopPreferences 8/8 定向测试通过，相关 ESLint 和 diff 空白检查通过。
- 网关初始化及开发服务连通，浏览器登录成功。
- 通过已有 adminApi 写入三个非默认批次值 1234/3456/4567，供后续检查普通保存不覆盖。
- 创建一个名称较长的测试代理节点，地址为测试用的本机未监听端口，供换行和连接失败反馈验证。

## 待核验

- [x] 四类导航、旧 `#section-cleanup` 定位、草稿保留与浏览器前进/后退；对应 `SystemSettings.spec.ts` 5 项通过。
- [x] 记录保留设置保存、刷新及隐藏批次原值保持：真实隔离网关确认 31、1234、3456、4567。
- [x] 代理节点列表与失败测试入口可达；测试节点使用隔离库的未监听本机端口，未触发外部网络。
- [x] 完整备份真实下载成功（`aether-data-2026-09-12.json`）；配置迁移沿用现有预览对话框和取消流程。
- [x] 维护入口默认折叠；清空配置确认弹窗可打开，取消后无写入。
- [x] Web 账号页仍保留账号、密码、登录设备；顶部主题/语言快捷按钮存在。桌面 IPC 由定向组件测试覆盖。
- [x] 840x620、1024x768、1440x900、390x844 均测得页面 `scrollWidth === clientWidth`；手机长节点名称自然换行。
- [x] 类型检查、修改文件 ESLint、定向 Vitest（本次相关 25 项；全套设置相关 80 项）及 `git diff --check` 通过。

未执行真实 macOS 原生端口重启、自启动写入，以及隔离库上的清空/恢复确认执行；这些路径保留现有实现并由组件测试覆盖，未宣称原生运行时验收。
