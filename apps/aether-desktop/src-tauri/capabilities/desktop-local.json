{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-local",
  "description": "Only the bundled desktop launcher can invoke desktop controls.",
  "windows": ["main"],
  "local": true,
  "permissions": ["core:default"]
}
