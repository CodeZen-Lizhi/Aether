# 请求详情前端布局

RequestDetailDrawer 保持取数、计费和传输标识逻辑，只调整头部展示、四项性能概览和折叠计费明细。HorizontalRequestTimeline 将实际候选/尝试统一为单级浏览，仍复用已有候选解析与状态来源；详情中每次显示一条，两侧悬浮箭头和底部分页。RequestAttemptErrorPanel 呈现当前记录的原因，详细响应按需展开。

不修改后端接口。真实错误分类与结果不因视觉方案被替换成固定原型数据。刷新按尝试身份保留选择，记录消失或新请求时回到合法选择。单记录隐藏无意义导航但保留面板结构。
