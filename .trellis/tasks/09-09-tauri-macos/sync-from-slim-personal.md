# 与主项目功能同步

2026-09-09 用户明确要求：将主项目的全部功能代码同步到 macOS 客户端分支，保留客户端特有改造，共用业务保持一致。

## 同步结果

- 来源：`slim-personal`，`a31c317366f6346735b36cef671e2db0e5cb8236`。
- 目标：`codex/tauri-macos`，由 `0942819e8` 快进到相同提交。
- 合入 `d3207c3fc`、`dcdf7ecf1`、`a31c31736`：移除模型映射单条添加、批量保存成功后关窗、取消备份版本门槛、完整原子导入及一致快照导出。
- 63 个更新文件中，60 个与来源逐字节一致；网关状态初始化、i18n 文案、网关规范索引三个交叉文件均无冲突合并。
- 同步前的 36 个已跟踪改动和 73 个未跟踪文件已做恢复快照。合并完成时逐文件核对确认客户端改造完整保留；后续客户端任务对偏好组件作的格式、类型声明和测试 fixture 调整也已保留，最终核对 63 个共享更新仍一致。
- 当前主工作区 `/Users/zhenglizhi/otherProjects/Aether` 保持干净。目标工作区继续保留尚未提交的客户端改造。

## 验证

- 前端 13 个测试文件、122 项测试通过，覆盖模型映射、备份、桌面 API 会话恢复、桌面页面、原 Web 鉴权和文案。
- 前端类型检查和生产构建通过。
- Rust 桌面会话 16 项、备份单元回归 21 项、真实备份路由 9 项测试通过；与前端合计 168 项。
- `cargo check --locked -p aether-desktop` 通过；验证使用共享构建目录，未替换原生安装包。
- 合并交叉文件的 ESLint、Rust 定向格式检查及 `git diff --check` 通过，无未解决冲突。
- 针对并发完成的三处客户端偏好调整，追加 8 项偏好测试、类型检查和三个文件的 ESLint，均通过。

日志与恢复快照：`/var/folders/fg/bzpd9ft96g976xqf_w4lwbrr0000gn/T/aether-macos-sync-ssqy2b2s/`。其中 `desktop-before.json` / `desktop-files-before.tar.gz` 记录同步前改造，`merge-verification.json` 记录逐文件保留与合并核验，测试及构建输出为同目录的各项 `.log`。

本记录覆盖代码同步及组合回归；客户端原生界面、安装包和完整交付继续按原任务验收。原安装包不代表这次同步后的代码。
