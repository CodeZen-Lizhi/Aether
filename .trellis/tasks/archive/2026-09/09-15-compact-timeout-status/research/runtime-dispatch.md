Active task: .trellis/tasks/09-15-compact-timeout-status
You are already the trellis-implement sub-agent. Implement directly; do not spawn other implement/check agents. User approved the final plan by saying 实施; task.py start succeeded and status is in_progress. Read artifacts/implement.jsonl via native injection or child fallback. Old planning wording in artifact history is superseded by explicit approval.

Ownership: apps/aether-gateway/src/execution_runtime/{chat_retry.rs,transport.rs,stream/*}, apps/aether-gateway/src/executor/{candidate_loop.rs,stream_path.rs}, related gateway entry points and targeted runtime/scheduler tests, apps/aether-tunnel/src and crates/aether-gateway/tunnel only if necessary. Do not edit provider admin configuration/summary, frontend, shared contracts or provider transport network.rs (timeout_config agent owns those), nor usage admin APIs/status frontend (status agent owns those), nor specs/task artifacts except your implementation report.

Goal: implement R2/R3 in approved PRD. HTTP chat/compaction stream final successful upstream headers end the per-attempt first-response deadline across watchdog/transport/prebody layers. No idle or first-effective-output timeout. Effective output remains protocol/precommit detection, not timeout. Independent total deadline from logical request start covers retries and response body pump after Response return, defaults900sec; response/heartbeat/retry never reset. Old stream_failover_budget_ms must not time out these HTTP chat streams, but preserve independent nonstream/WS/image semantics. Existing first-response30 default and saved values remain. For tunnel, relay HTTP200 is not upstream200; use decoded real upstream headers and ensure cancellation/resource release. Do not weaken stream error/EOF/commit/retry invariants. Total deadline terminal persistence has single owner and survives same deadline cancellation.

Config agent interface: aether_contracts::chat_retry::DEFAULT_STREAM_TOTAL_TIMEOUT_MS=900000; resolve_stream_total_timeout_ms(config: Option<&Value>)->u64; ExecutionTimeouts.stream_total_ms:Option<u64>; resolver at provider/transport/src/network.rs fills it. config root stream_total_timeout_ms valid1000..1200000. Code can reference these names while agent adds them. Coordinate needed shared contract changes with parent, don't modify its files yourself.

Meaningful tests: actual local upstream through gateway route: headers100ms, first1s, body/text or compaction3s, total5s succeeds even old1.8s budget; headers then no body only total failure; no headers first failure; output continues beyond total expires; error before/after commit and EOF/cancel; actual SQLite usage/candidate terminal and followup resources. Fix impacted old tests requiring semantic-first deadlines. Use test prefix stream_response_timeout for new targeted cases. Don't run cargo builds until coordinate parent (shared target lock); run static focused checks or request a single verification slot. No commits, production calls, installation. Deliver code plus concise file/test/risk report; carry work through runtime correctness rather than stopping at helpers.
