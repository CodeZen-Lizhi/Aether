# 仪表盘重整验证

## 范围与结果

仅修改前端，复用 stats、daily-stats、time-series；没有新增后端接口、依赖或真实数据库操作。累计、今日、所选周期分层展示，默认今天按小时，不包含平台拆分、请求日志或月度健康区。

## 自动检查与审查

- Dashboard 18 项：默认 today/hour、累计与今日范围独立、平均响应秒数与无数据状态、独立失败重试、旧范围请求失效、图例、历史缺失、排行排序和完整列表展开。
- usageTrend 6 项：五类数据、费用来源、部分与全部明细缺失、跨年 ISO 周 / 月、小时标签与缺失时段、非法数值。
- lifetime API 4 项：保留历史加原始记录累计一次、空历史为零、非法部分结果整体失败并重试、时区及跨午夜缓存。
- analytics 缓存 2 项与 i18n 6 项通过。合计 36 项；复用已通过的未改动范围证据，没有重复无关后端测试。
- 全部变更前端文件 ESLint、vue-tsc -b 和 git diff --check 通过；后续纯配色修复再次通过对应文件 ESLint 和浏览器检查。
- 独立审查修复：每日舍入差异误报历史缺失；跨日小额费用累加后丢失并影响排序；英文界面误翻译真实中文模型 / 提供商名称。相应回归先复现再验证修复。
- 浏览器进一步发现 Tailwind 完整颜色变量不支持本项目的透明度后缀，导致排行条透明；已用有效主题色及独立 opacity，累计背景使用项目已有的 color-mix。实际 computed style 和明暗截图均确认生效。

## 隔离数据验收

独立 SQLite 中生成 30 条原始请求及 9 条仅保留总量的历史记录，未访问真实客户端数据。

- 累计：39 请求、1,569,000,000 Token、费用 $168.00、实际结算 $117.60。保留历史费用 $24.00、原始费用 $144.00；没有重复累计缺失部分。
- 累计完整范围返回 20,707 个日桶；带空白 JSON 约 4.8 MB，实测 87 ms，前端仅缓存压缩后对象。费用精度仍遵循既有每日四位小数接口契约。
- 今天：6 请求、153M Token、费用 $18.00。净输入 24M、输出 6M、缓存创建 3M、缓存命中 120M，与今日排行总量一致。原始 OpenAI input 含缓存，趋势按接口原口径显示并有说明。
- 今日耗时为 1 / 2 / 3 / 4 / 5 / 84.84 秒，最后一条失败；总计 99.84 秒，平均 16.64s，成功 5 / 失败 1。切换昨天（平均 800ms）后顶部仍为 16.64s，累计也保持不变。
- 峰值日：输入 504M、输出 18M、缓存创建 12M、缓存命中 420M、费用 $48.00。仅历史总量日保留 $24.00，Token 明细留空，不伪装成零或分配到小时。

## 浏览器与响应式

- 重新打开最终页面默认今天 / 小时、明细折叠，五个图例可操作，缓存命中为面积曲线；最近 7 天自动转为按天。
- 真实模型 / 提供商名称、Token / 费用 / 请求排序显示正常；两组历史缺口各自单独展示。展开全部由组件交互测试覆盖。
- 检查中文 / 英文和浅色 / 深色。英文今日卡片、动态 Token 说明、累计、排行、图表与无障碍标签翻译正常；真实业务名称保持原文。
- 展开明细测量文档 / 主区域 / 趋势卡片，所有 scrollWidth <= clientWidth，且主区域内无溢出元素：

| 窗口 | 文档宽 | 主区域宽 | 趋势卡片宽 |
| --- | ---: | ---: | ---: |
| 840×620 | 840 | 840 | 804 |
| 1024×768 | 1024 | 764 | 721 |
| 1280×900 | 1280 | 1020 | 970 |
| 1920×1080 | 1920 | 1660 | 1610 |

- 840 px 英文深色且展开明细仍无溢出；最终页面刷新后没有控制台 error。排行条浅色 rgb(37,99,235)、深色 rgb(59,130,246)，opacity 0.7，确认不是只存在无效类名。
- 浏览器语言、主题、viewport 已恢复，自己创建的标签已关闭。

## 交付状态

使用趋势：macOS `0d9964f74` / slim `5c76535dc`。本轮仪表盘重整与今日响应：macOS `b3f1ecfb3` / slim `c8f28cb04`，已 atomic push。受影响的通用源码完全一致，i18n 和规格中仅保留原有桌面特有差异。

从干净的 `b3f1ecfb3c83fac6c3a2c5208c0167b18f2ad887` 构建安装包。包内前端与本次 dist 完全一致，累计、今日响应、排行和小时趋势均存在；DMG 只读挂载后 117 个包内文件与原 app 一致，深度严格签名与 DMG 校验通过。架构 arm64，最低 macOS 14，ad-hoc 签名，未公证。

- 安装包：`/Users/zhenglizhi/Downloads/Aether_0.1.0_aarch64_20260910_b3f1ecfb3.dmg`
- 大小：31,537,728 字节。
- SHA256：`517e6268c641f32b893cf46735300ff8f8ff4a33a96f9799f06c9a90cf8ac1f9`，复制后再次匹配。
- 包内网关 SHA256：`7ed710f0005130cc1050c632958811e77493643c5b412577f3eaf5055e306ac7`，与此前通过完整生命周期验收的二进制一致，复用该证据。未重新执行无变化的原生宿主与网关生命周期检查。
- 本次隔离服务已正常停止；真实运行中的客户端和数据未操作。
