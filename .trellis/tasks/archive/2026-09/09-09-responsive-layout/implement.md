# 执行顺序

1. 建立隔离页面验收环境，记录主要页面和表格的横向溢出。
2. 修正公共布局、表格和请求记录宽度，统一滚动条策略。
3. 检查主要页面和详情 / 编辑器，修复剩余溢出及操作可达性。
4. 运行受影响测试、类型检查、定向 ESLint 和尺寸矩阵验收；检查全部变更与前端规范。
5. 补充必要规范和验收记录，提交、同步并 push 两分支。
6. 构建客户端，验证签名、资源一致性、隔离启动后交付安装包并归档。

回滚以本任务的功能提交为边界，不涉及数据迁移。

## 执行状态

- 步骤 1～4 已完成。133 项相关测试通过，最终模板调整后的 49 项使用记录及请求详情测试再次通过；类型检查、所有修改文件的 ESLint、git diff --check 均通过。
- 8 个页面 × 4 个尺寸的隔离浏览器验收通过，复杂组件及交互另行核验，详见 verification.md。
- 步骤 5～6 已完成。共用提交 0793abfd3、客户端入口提交 9b1501081；共用修改同步到 slim-personal 的 a83d4a233，两个分支已 push。32 个受影响的共用前端文件一致。
- 使用 9b1501081 的干净源码构建安装包，资源一致性、签名、DMG 和隔离网关生命周期验证通过，已复制到 Downloads，详见 verification.md。
