# 验收记录

## 页面与滚动

使用正式网关代码、临时 SQLite 和合成账号 / 供应商 / 模型 / 使用记录；没有读写真实客户端数据库。

| 页面 | 840×620 | 1024×820 | 1280×820 | 1920×820 |
| --- | --- | --- | --- | --- |
| 仪表盘 | 通过 | 通过 | 通过 | 通过 |
| 供应商 | 通过 | 通过 | 通过 | 通过 |
| 模型 | 通过 | 通过 | 通过 | 通过 |
| 路由 | 通过 | 通过 | 通过 | 通过 |
| 使用记录 | 通过 | 通过 | 通过 | 通过 |
| 密钥 | 通过 | 通过 | 通过 | 通过 |
| 系统配置 | 通过 | 通过 | 通过 | 通过 |
| 设置 | 通过 | 通过 | 通过 | 通过 |

全部测量页面与可滚动容器的 scrollWidth/clientWidth，没有横向溢出，滚动条样式为 none。使用记录包含 24 条当日合成记录并选中全部 12 列，长名称及 User-Agent 完整保留；布局按实际容器宽度切换。装饰性呼吸伪元素和点击热区没有造成父容器或业务内容溢出。

## 边界和交互

- 请求详情：1000 多字符 URL / JSON 完整显示末尾标记 VISIBLE-DETAIL-END，键盘 End 可将详情滚至最大位置。
- 最小窗口模型编辑：底部保存按钮可见，合成描述保存成功后弹窗关闭。
- 请求链路：12 个节点，第一个节点 24 次尝试，23 个重试按钮均存在，最后一次尝试仍可点击并打开正确详情。
- 图片价格矩阵：high 价格输入修改为 0.25，组件输出正确，摘要完整可读。
- 35 层深 JSON、长请求头及图片价格摘要无横向溢出；部分组件另在 390 px 宽度核验。
- 请求头左右对比：一侧长值换行后，两侧首行高度同为 420 px，后续行顶边对齐，增删改状态和旧值保留。

## 自动检查

133 项相关测试通过：使用记录 38、请求链路 21、价格编辑 37、模型表单 8、仪表盘 2、使用记录初始加载 2、供应商详情 2、请求详情计费 11、价格摘要 3、批量映射 6、公共 badge / checkbox / SidebarNav 各 1。最终请求头模板和卡片条件调整后，使用记录与请求详情的 49 项测试再次通过。

frontend 类型检查、所有修改文件 ESLint、git diff --check 均通过。新增测试覆盖可见列总宽 100%、DOM 顺序和两种布局中的完整长 User-Agent / IPv6。逐项审查公共布局、列表、详情及客户端入口，未发现需阻止交付的问题。

## 分支与安装包

- 共用功能提交：0793abfd3；macOS 客户端入口提交：9b1501081；slim-personal 同步提交：a83d4a233。两个分支已 push，本次 32 个共用前端文件内容一致；保留原有客户端专属差异。
- 构建命令：npm --prefix apps/aether-desktop run build。构建及验包期间源码干净，构建提交为 9b1501081df3339af00306e0d83063ff8bb2298f。
- 包内前端与本次 frontend/dist 完全一致，包含容器布局切换、完整错误换行和请求头共享行实现。DMG 挂载后的 117 个应用文件与构建产物一致，Applications 快捷入口存在。
- codesign 深度严格校验、DMG 校验通过。网关和客户端均为 arm64，仅链接系统动态库；最低 macOS 14.0，ad-hoc 签名，未公证。
- 使用包内网关与前端资源运行隔离 qa_lifecycle.py：供应商 / 端点 / 加密密钥 / 模型映射 / 客户端密钥、JSON / SSE 转发、会话刷新、配置导入导出、8 个管理路由均通过。stdin EOF、SIGTERM、父进程退出与数据持久化检查通过，无遗留网关进程。
- 本次页面验收标签页已关闭、viewport 覆盖已恢复，临时 Vite / 网关服务已停止，组件验收 HTML 已移出仓库。
- 安装包：`/Users/zhenglizhi/Downloads/Aether_0.1.0_aarch64_20260910_9b1501081.dmg`，31,616,747 bytes。
- SHA-256：`ab7585626369fb4d16c8d946b81de42b8ce4fb307f0eeae20fc2a2cc1e65c6ee`。
