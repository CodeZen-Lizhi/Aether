# 复现、验证与审查

## 修复前报红

- 两个前端导入入口：先获取空代理列表，模拟服务端导入成功后返回一个节点，store 仍保持 `[]`。
- 跨实例相同地址不同 ID：列表 API 有节点，但 `system_proxy_node_id` 仍为源 ID。
- 渠道状态导出：实际 SQLite 已有 `learned_rpm_limit = 40` 等数据，导出缺字段。

## 已通过

- 前端相关测试共 76 个（设置、导入超时、代理 store）；新增的最后一个刷新失败用例在包含 36 个测试的定向复跑中通过。
- 前端 `vue-tsc -b`；变更文件 ESLint；Rust 变更文件 rustfmt；`git diff --check`。
- 网关真实 SQLite API 12 个测试：完整往返、停用实体、凭据跨密钥重加密、全部代理关联重映射、失败回滚、旧备份兼容。
- 新增状态无效类型、溢出和负成本验证后，相关往返测试再次通过。
- 网关 backup 范围 14 个单元测试：提交收尾、取消、缓存与 mutation lease、旧内容兼容。
- aether-admin system 范围 32 个单元测试。
- SQLite 备份事务专项 6 个测试：共享仓储、快照、取消、后段失败及外键提交失败回滚。

## 聚焦审查

- 仅备份恢复路径更新渠道运行状态，常规管理员编辑的 CAS/运行时所有权边界未改变。
- 新字段同属原有 SQLite 事务；强制后段状态写入失败，比较全部配置/用户/会话表证明没有局部提交。
- 老备份缺字段时保留目标已有状态，新备份显式 null/zero 会覆盖；停用数据仍导出。
- 导入提交后才刷新页面；刷新失败不会把已经提交的导入误报为数据库失败。
- 代理分页、旧响应覆盖、重复添加后的列表恢复、已吊销会话均有回归。
- 测试全部使用临时 SQLite 和合成凭据，没有读写真实业务库。库测试有既有 `dispatch/refs.rs` unreachable-pattern warning，与本次无关。

## 收尾

- 共 140 个范围内测试通过；客户端正式包额外通过独立运行验收。
- 代码提交：`bbab15f05`、`8670776fc`；已同步到 slim-personal 的 `730742966`、`099bfae56` 并原子 push 两分支。
- 比较修复前后两分支的完整差异（忽略 diff 行号和对象 ID），客户端专属差异完全一致，没有新增业务偏差。
- 安装包：`/Users/zhenglizhi/Downloads/Aether_0.1.0_aarch64_8670776fc.dmg`，31,657,973 字节，arm64 / macOS 14+，本地 ad-hoc 签名，未 Apple 公证。
- SHA256：`104bf44991e8065924856dab1adff0a8e9c155e7104d3a26c7d5db470369a31d`。
- codesign 严格校验、DMG 校验、只读挂载内 117 个文件与构建 App 比对通过；系统库依赖和架构通过。
- 随包正式网关在临时数据库通过配置往返、JSON/SSE、会话续期、八个管理路由、退出与持久化；正式导出包含新增渠道字段且无格式版本。随包前端也已包含节点刷新和缓存失效代码。
- 构建与验收记录在 `/var/folders/fg/bzpd9ft96g976xqf_w4lwbrr0000gn/T/aether-backup-proxy-release-3txsm5ne/`；未启动或修改真实客户端的数据。
