# 实施与验证

1. 保留前端空缓存复现、后端代理引用复现为回归，补充供应商信息完整往返断言。
2. 修复两个导入入口的缓存失效与设置刷新；显示节点加载/错误态并可重试。
3. 统一系统代理引用校验和映射，补齐渠道状态导出与事务恢复。
4. 运行受影响前端测试、类型检查、lint；运行 Rust 备份、仓储与原子回滚回归，完成聚焦审查。
5. 更新规范与证据，提交并同步共用分支；重新打包并校验安装包。
