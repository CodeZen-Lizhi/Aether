Active task: .trellis/tasks/09-12-settings-stable-layout

你已经是 trellis-check 子代理，直接审查并修复，不要再派发 implement/check 或创建其他任务。用户已批准实施，主会话负责浏览器验证和 spec/任务文档收尾；你负责本次所有前端 diff 的独立审查。

阅读当前 prd/design/implement 及 check.jsonl 的相关规范。使用 code-review-and-quality 做聚焦审查。仅检查这次设置页、局部 CSS、桌面展示、翻译和测试的 13 个前端文件，不审查 output/ 或其他任务，不扩大后端变更、不提交、不打包。

实现子代理已报告 6 个相关测试文件 79 项通过、type-check、定向 ESLint、diff --check 通过。主会话已用真实隔离 HTTP 后端在 900/1024/1200/1440/1920 尺寸及连续缩放验证结构；代理保存读回、返回草稿、备份生成、恢复预览取消通过；端口/自启动用模拟 IPC 的成功失败状态通过。原生宿主尚未实际验收。

主会话刚修复两个视觉问题：添加「网关」英文映射；设置行右列扩大为 280-320px，让英文备份按钮一行显示。该尺寸微调符合用户只压缩留白的目标，设计文档随后更新，不要回退至 240-280px。

重点：高级分组 basic 共享保存边界、旧 query/hash 自动展开及滚动、返回与前进后退草稿保留、视图拆分有无重复挂载导入输入/节点弹窗及错误传播、桌面 recovery 无回归、Web 无桌面命令、清理批次参数保持、i18n。允许直接修复已确认范围内缺陷并加最小回归测试。主会话继续浏览器检查，不要占用或更改 settings-stable Playwright 会话与测试服务器。若需改代码先通过 channel 告知主会话，便于避开 HMR。

修复后只跑所需检查，不全仓测试或全仓 lint --fix。返回已修复/仍存在的问题、证据和检查结果；若无实质问题明确说明。
